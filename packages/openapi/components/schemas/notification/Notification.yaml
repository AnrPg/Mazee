type: object
properties:
  id: { $ref: "../primitive/ID.yaml" }
  kind: { type: string, enum: [friend_request, message, post_like, appointment] }
  actor: { $ref: "../profile/Profile.yaml" }
  data: { type: object, additionalProperties: true }
  createdAt: { $ref: "../primitive/ISODateTime.yaml" }
  read: { type: boolean }
