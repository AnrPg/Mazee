type: object
required: [kind]
properties:
  kind: { type: string, enum: [text, image, video] }
  text: { type: string, maxLength: 4000 }
  mediaUploadIds:
    type: array
    items: { $ref: "../primitive/ID.yaml" }
  audience: { type: string, enum: [public, synaxia, private], default: public }
