type: object
properties:
  id: { $ref: "../primitive/ID.yaml" }
  author: { $ref: "../profile/Profile.yaml" }
  kind: { type: string, enum: [text, image, video] }
  text: { type: string, nullable: true }
  media:
    type: array
    items: { $ref: "../media/Media.yaml" }
  stats:
    type: object
    properties:
      likes: { type: integer }
      comments: { type: integer }
  createdAt: { $ref: "../primitive/ISODateTime.yaml" }
