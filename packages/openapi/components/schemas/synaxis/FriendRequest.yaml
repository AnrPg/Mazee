type: object
properties:
  id: { $ref: "../primitive/ID.yaml" }
  from: { $ref: "../profile/Profile.yaml" }
  to: { $ref: "../profile/Profile.yaml" }
  status: { type: string, enum: [pending, accepted, declined] }
  createdAt: { $ref: "../primitive/ISODateTime.yaml" }
